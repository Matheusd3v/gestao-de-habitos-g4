import './style.js'
import { Button } from './style.js'
const ButtonDefault = ({ children }) =>{
    return (
        <Button>
            { children }
        </Button>
    )
}

export default Button